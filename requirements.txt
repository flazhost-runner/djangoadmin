Django==6.0.6
bcrypt==5.0.0
# DB drivers — pure-Python / pure-wheel so the runtime image needs no
# libmysqlclient-dev/gcc build deps. PyMySQL is registered as MySQLdb in
# config/settings/__init__.py (django.db.backends.mysql); psycopg2-binary
# backs django.db.backends.postgresql. Without these, DB_TYPE=mysql/postgres
# fails with "Error loading MySQLdb module" and the app falls back to per-replica
# SQLite → login breaks when scaled to >1 replica.
PyMySQL==1.1.1
psycopg2-binary==2.9.10
djangorestframework==3.17.1
django-environ==0.14.0
whitenoise==6.12.0
django-cors-headers==4.9.0
PyJWT==2.13.0
Pillow==12.2.0
bleach==6.4.0
django-ratelimit==4.1.0
pytest==9.1.1
pytest-django==4.12.0
pytest-cov==7.1.0
behave==1.3.3
behave-django==2.0.0
ruff==0.15.20
mypy==2.1.0
django-stubs==6.0.6
djangorestframework-stubs==3.17.0
